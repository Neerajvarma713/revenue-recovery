import {useEffect,useState} from 'react';import {api} from '../services/api';import RiskStamp from '../components/RiskStamp';const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n||0);
export default function Customers(){const[q,setQ]=useState('');const[rows,setRows]=useState([]);const load=()=>api.customers(q).then(setRows);useEffect(load,[q]);return <><div className="mb-7"><div className="mono text-[10px] muted tracking-widest">CUSTOMER LEDGER</div><h1 className="serif text-4xl mt-2">Customers</h1></div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search customer name…" className="w-full bg-white/50 border line px-4 py-3 outline-none mb-4 text-sm"/><div className="bg-white/50 border line"><table className="w-full text-sm"><thead className="mono text-[9px] muted text-left border-b line"><tr><th className="p-4">CUSTOMER</th><th>PLAN</th><th>MONTHLY</th><th>CHURN</th><th>RISK</th></tr></thead><tbody>{rows.map(c=><tr className="border-b line last:border-0"><td className="p-4 font-semibold">{c.name}</td><td>{c.planType}</td><td>{money(c.monthlyRevenue)}</td><td className="mono text-xs">{((c.churnProbability||0)*100).toFixed(0)}%</td><td><RiskStamp level={c.riskLevel}/></td></tr>)}</tbody></table></div></>}
