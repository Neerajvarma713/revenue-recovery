import {useEffect,useState} from 'react';import {api} from '../services/api';const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n||0);
export default function Interventions(){const[r,setR]=useState([]);useEffect(()=>{api.interventions().then(setR)},[]);return <><div className="mb-7"><div className="mono text-[10px] muted tracking-widest">ACTION BOOK</div><h1 className="serif text-4xl mt-2">Interventions</h1><p className="muted mt-2">Recommended actions are evaluated on expected retention value before approval.</p></div><div className="grid gap-3">{r.length?r.map(x=><div className="bg-white/50 border line p-5 flex justify-between"><div><div className="font-semibold">{x.strategy}</div><div className="text-xs muted mt-2">{x.reason}</div></div><div className="text-right"><div className="mono text-[9px] muted">NET VALUE</div><div className="serif text-xl">{money(x.netValue)}</div></div></div>):<div className="border line p-10 text-center muted">No intervention records yet. Open a customer and run recommendations.</div>}</div></>}
