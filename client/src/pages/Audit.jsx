import {useEffect,useState} from 'react';import {api} from '../services/api';export default function Audit(){const[r,setR]=useState([]);useEffect(()=>api.audit().then(setR),[]);return <><div className="mb-7"><div className="mono text-[10px] muted tracking-widest">CONTROL LOG</div><h1 className="serif text-4xl mt-2">Audit trail</h1></div><div className="bg-white/50 border line divide-y line">{r.length?r.map(x=><div className="p-4 flex justify-between"><div><b>{x.action}</b><div className="mono text-[10px] muted mt-1">{x.entityType} · {x.entityId}</div></div><span className="mono text-[10px] muted">{new Date(x.createdAt).toLocaleString()}</span></div>):<div className="p-10 text-center muted">No audit events yet.</div>}</div></>}
