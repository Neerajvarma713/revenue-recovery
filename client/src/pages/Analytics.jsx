import {useEffect,useState} from 'react';import {api} from '../services/api';const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n||0);export default function Analytics(){const[d,setD]=useState();useEffect(()=>api.analytics().then(setD),[]);if(!d)return <div className="muted">Loading analytics…</div>;return <><div className="mb-8"><div className="mono text-[10px] muted tracking-widest">PORTFOLIO ANALYSIS</div><h1 className="serif text-4xl mt-2">Analytics</h1></div><div className="bg-[#193532] text-[#f1eee6] p-7 mb-5"><div className="mono text-[9px] opacity-60">TOTAL ESTIMATED REVENUE AT RISK</div><div className="serif text-4xl mt-2">{money(d.revenueAtRisk)}</div></div><div className="bg-white/50 border line p-6"><h2 className="serif text-2xl mb-5">By plan</h2>{d.byPlan.map(x=><div className="grid grid-cols-3 py-4 border-b line"><span className="font-semibold capitalize">{x.plan}</span><span className="mono text-xs">{x.count} customers</span><span className="text-right">{money(x.atRisk)}</span></div>)}</div></>}
